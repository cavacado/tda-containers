---
apiVersion: v1
kind: Service
metadata:
  name: bggapp-svc
  namespace: bggns
  labels:
    app: bggapp
    name: bggapp-svc
spec:
  type: ClusterIP
  selector:
    app: bggapp
    name: bggapp-po
  ports:
  - port: 5000
    targetPort: 5000